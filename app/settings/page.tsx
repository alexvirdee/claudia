import { SettingsPanel } from "@/components/settings/settings-panel";

export const metadata = {
  title: "Claudia — Settings",
};

export default function SettingsPage() {
  return <SettingsPanel />;
}
